Runtime state for the `codex-sdlc` plugin.

- `runtime/turns/` is created on demand by hook scripts.
- Files under `runtime/` are ephemeral and ignored.
- Keep durable workflow policy in plugin skills, hook scripts, and `AGENTS.md`, not here.
